<nav class="nav">
  <ul class="nav__list">
    {{#foreach navigation}}
      <li class="nav__list-item nav__list-item--{{slug}}{{#if current}} nav__list-item--current{{/if}}" role="presentation">
        <a href="{{url absolute="true"}}">
          {{label}}
        </a>
      </li>
    {{/foreach}}
  </ul>
</nav>